import React from "react";
import { PrimaryButton, SecButton } from "../lib";

const App = () => (
    <div>
        <PrimaryButton></PrimaryButton>
        <SecButton></SecButton>
        <h1>Hello React</h1>
    </div>
);

export default App;